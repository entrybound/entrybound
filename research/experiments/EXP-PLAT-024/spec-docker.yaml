# ebr spec (schema ebr.spec.v1) -- Phase C-design pre-registration DRAFT, platform domain.
# Not executable as decision evidence until the gates in protocol.md are met; scripts named in
# `command` are specified in protocol.md (Commands) and are written before the first run.
schema: ebr.spec.v1
experiment_id: EXP-PLAT-024
status: blocked-platform
question: 'Docker parity: do Docker''s runc, moby seccomp profile, userns-remap and overlay2 change capture,
  containment and whiteout outcomes?'
hypothesis: Same as the podman route, with moby-profile-specific openat2 handling.
decision_ids:
- DEC-PLT-032
- DEC-PLT-005
- DEC-PLT-004
- DEC-PLT-046
- DEC-PLT-017
- DEC-MOD-002
- DEC-PLT-035
- DEC-PLT-062
candidates:
- name: docker-ownership
  params:
    runtime: docker
    arm: ownership
- name: docker-confinement
  params:
    runtime: docker
    arm: confinement
- name: docker-whiteouts
  params:
    runtime: docker
    arm: whiteouts
- name: docker-noproc
  params:
    runtime: docker
    arm: noproc
command: bash /mnt/d/Projects/entrybound/entrybound/research/experiments/EXP-PLAT-024/scripts/docker/container_arms.sh
  --runtime {runtime} --arm {arm} --item {item_path} --item-id {item_id} --scratch {scratch}
shell: bash
corpus:
  splits:
  - tuning
  - validation
  manifest: research/corpus/manifest.json
  item_ids:
  - f16-oci-ubuntu-noble-multiarch-index
  - f16-oci-fedora-44-zstd-layers
  generated:
  - item_id: plat024-fixture-tuning
    family: synthetic-trigger
    generator: zeros-v1
    bytes: 1
    seed: 20260917
  - item_id: plat024-fixture-validation
    family: synthetic-trigger
    generator: zeros-v1
    bytes: 1
    seed: 20260918
metrics:
- name: capture_fidelity_fraction
  source: stdout_json
  key: payload.capture_fidelity_fraction
  family: other
  direction: maximize
  unit: fraction
- name: restore_fidelity_fraction
  source: stdout_json
  key: payload.restore_fidelity_fraction
  family: other
  direction: maximize
  unit: fraction
- name: hc05_mvt05_escapes
  source: stdout_json
  key: payload.hc05_mvt05_escapes
  family: correctness
  direction: minimize
  unit: count
- name: hc15_confinement_misreports
  source: stdout_json
  key: payload.hc15_confinement_misreports
  family: correctness
  direction: minimize
  unit: count
- name: unsafe_defaults
  source: stdout_json
  key: payload.unsafe_defaults
  family: correctness
  direction: none
  unit: count
- name: observation_sha256
  source: stdout_json
  key: payload.observation_sha256
  family: correctness
  kind: string
repetitions: 2
warmups: 0
record_warmups: true
seeds:
  order: 20260917
  bootstrap: 20260917
  command: 20260917
timing: false
order: sequential
raw_compression: gzip
platform:
  os:
  - linux
  arch:
  - x86_64
  emulated: false
  requires:
  - docker
  - /root/eb-research/target/production/release/ebound
  timeout_s: 7200
  scratch_root: /root/eb-research/scratch/platform
analysis:
  baseline: docker-ownership
  pairing: item
  report_metrics:
  - capture_fidelity_fraction
  - restore_fidelity_fraction
  - hc05_mvt05_escapes
  - hc15_confinement_misreports
  - unsafe_defaults
