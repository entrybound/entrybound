schema: ebr.spec.v1
experiment_id: EXP-CODEC-001
status: pre-registration-draft (gate G-A pending; protocol.md section 1)
notes: 'Generated by make_specs.py. Stage B (exact portfolio cost), tuning split. Universe = configs/universe-stageB.json written by screen.py. Flags marked TOOLING in protocol.md section 7.2 do not exist yet; runs refuse until built (CC-1).'
question: 'At production chunk scope with exact-chunk dedup, what artifact_bytes does each pre-registered codec portfolio (baseline sets, extended additions, Zstandard window policies) need on every tuning item under the production v4 qualification rule?'
hypothesis: 'H1a-H1e of protocol.md section 3 (for example LZMA2 in the baseline is distinguishable but below the section 7.3 baseline-codec minimum gain at corpus level; LZ4 adds no size; windows above log 20 are equivalent for independent chunks). Controls: PCM equals real encodes for production-expressible portfolios; zero round-trip mismatches.'
decision_ids: [DEC-CMP-008, DEC-CMP-009, DEC-CMP-045]
candidates:
  - name: b-sq-fast
    params: {portfolio: B_SQ, profile: fast}
  - name: b-sq-balanced
    params: {portfolio: B_SQ, profile: balanced}
  - name: b-sq-dense
    params: {portfolio: B_SQ, profile: dense}
  - name: b-sq-extreme
    params: {portfolio: B_SQ, profile: extreme}
  - name: b-store-fast
    params: {portfolio: B_STORE, profile: fast}
  - name: b-store-balanced
    params: {portfolio: B_STORE, profile: balanced}
  - name: b-store-dense
    params: {portfolio: B_STORE, profile: dense}
  - name: b-store-extreme
    params: {portfolio: B_STORE, profile: extreme}
  - name: b-zstd-fast
    params: {portfolio: B_ZSTD, profile: fast}
  - name: b-zstd-balanced
    params: {portfolio: B_ZSTD, profile: balanced}
  - name: b-zstd-dense
    params: {portfolio: B_ZSTD, profile: dense}
  - name: b-zstd-extreme
    params: {portfolio: B_ZSTD, profile: extreme}
  - name: b-zstd-lz4-fast
    params: {portfolio: B_ZSTD_LZ4, profile: fast}
  - name: b-zstd-lz4-balanced
    params: {portfolio: B_ZSTD_LZ4, profile: balanced}
  - name: b-zstd-lz4-dense
    params: {portfolio: B_ZSTD_LZ4, profile: dense}
  - name: b-zstd-lz4-extreme
    params: {portfolio: B_ZSTD_LZ4, profile: extreme}
  - name: b-zstd-lzma2-dense
    params: {portfolio: B_ZSTD_LZMA2, profile: dense}
  - name: b-zstd-lzma2-extreme
    params: {portfolio: B_ZSTD_LZMA2, profile: extreme}
  - name: b-deflate-zstd-fast
    params: {portfolio: B_DEFLATE_ZSTD, profile: fast}
  - name: b-deflate-zstd-balanced
    params: {portfolio: B_DEFLATE_ZSTD, profile: balanced}
  - name: b-deflate-zstd-dense
    params: {portfolio: B_DEFLATE_ZSTD, profile: dense}
  - name: b-deflate-zstd-extreme
    params: {portfolio: B_DEFLATE_ZSTD, profile: extreme}
  - name: b-deflate-fast
    params: {portfolio: B_DEFLATE, profile: fast}
  - name: b-deflate-balanced
    params: {portfolio: B_DEFLATE, profile: balanced}
  - name: b-deflate-dense
    params: {portfolio: B_DEFLATE, profile: dense}
  - name: b-deflate-extreme
    params: {portfolio: B_DEFLATE, profile: extreme}
  - name: e-lzma2-xz-dense
    params: {portfolio: E_LZMA2_XZ, profile: dense}
  - name: e-lzma2-xz-extreme
    params: {portfolio: E_LZMA2_XZ, profile: extreme}
  - name: e-brotli-dense
    params: {portfolio: E_BROTLI, profile: dense}
  - name: e-brotli-extreme
    params: {portfolio: E_BROTLI, profile: extreme}
  - name: e-bzip2-dense
    params: {portfolio: E_BZIP2, profile: dense}
  - name: e-bzip2-extreme
    params: {portfolio: E_BZIP2, profile: extreme}
  - name: e-ppmd-dense
    params: {portfolio: E_PPMD, profile: dense}
  - name: e-ppmd-extreme
    params: {portfolio: E_PPMD, profile: extreme}
  - name: e-bsc-dense
    params: {portfolio: E_BSC, profile: dense}
  - name: e-bsc-extreme
    params: {portfolio: E_BSC, profile: extreme}
  - name: e-cm-extreme
    params: {portfolio: E_CM, profile: extreme}
  - name: e-flac-dense
    params: {portfolio: E_FLAC, profile: dense}
  - name: e-flac-extreme
    params: {portfolio: E_FLAC, profile: extreme}
  - name: e-sci-dense
    params: {portfolio: E_SCI, profile: dense}
  - name: e-sci-extreme
    params: {portfolio: E_SCI, profile: extreme}
  - name: e-openzl-dense
    params: {portfolio: E_OPENZL, profile: dense}
  - name: e-openzl-extreme
    params: {portfolio: E_OPENZL, profile: extreme}
  - name: w-per-profile-fast
    params: {portfolio: W_PER_PROFILE, profile: fast}
  - name: w-per-profile-balanced
    params: {portfolio: W_PER_PROFILE, profile: balanced}
  - name: w-ldm-dense
    params: {portfolio: W_LDM, profile: dense}
  - name: w-ldm-extreme
    params: {portfolio: W_LDM, profile: extreme}
  - name: w-chunk-fast
    params: {portfolio: W_CHUNK, profile: fast}
  - name: w-chunk-balanced
    params: {portfolio: W_CHUNK, profile: balanced}
  - name: w-chunk-extreme
    params: {portfolio: W_CHUNK, profile: extreme}
command: >-
  /root/eb-research/target/harness/release/ebr-planner portfolio --item-path {item_path} --experiment-id EXP-CODEC-001
  --env-name wsl-ubuntu --profile {profile} --portfolio {portfolio}
  --portfolios /mnt/d/Projects/entrybound/entrybound/research/experiments/EXP-CODEC-001/configs/portfolios.json
  --universe /mnt/d/Projects/entrybound/entrybound/research/experiments/EXP-CODEC-001/configs/universe-stageB.json
  --plan-record-model /mnt/d/Projects/entrybound/entrybound/research/experiments/EXP-CODEC-001/configs/plan-record-model.json
  --selection-rule production-v4 --reconcile-expressible true
  --matrix-cache /root/eb-research/cache/EXP-CODEC-001/stageB/{item_id}-{profile}-r{rep}.jsonl.gz
  --detail-out /root/eb-research/raw-detail/EXP-CODEC-001/stageB/{item_id}/{candidate}-r{rep}.jsonl.gz
  --summary-stdout
shell: bash
corpus:
  manifest: research/corpus/manifest.json
  splits: [tuning]
metrics:
  - {name: input_bytes, source: input, family: size, direction: none, unit: B}
  - {name: artifact_bytes, source: stdout_json, key: payload.artifact_bytes, family: size, direction: minimize, unit: B}
  - {name: artifact_bytes_is_exact, source: stdout_json, key: payload.artifact_bytes_is_exact, family: correctness, direction: none, unit: bool}
  - {name: pcm_reconciliation_mismatches, source: stdout_json, key: payload.pcm_reconciliation_mismatches, family: correctness, direction: minimize, unit: count}
  - {name: metadata_bytes, source: stdout_json, key: payload.byte_accounting.metadata_bytes, family: size, direction: minimize, unit: B}
  - {name: index_bytes, source: stdout_json, key: payload.byte_accounting.index_bytes, family: size, direction: minimize, unit: B}
  - {name: chunk_payload_bytes, source: stdout_json, key: payload.chunk_payload_bytes, family: size, direction: minimize, unit: B}
  - {name: plan_record_bytes, source: stdout_json, key: payload.plan_record_bytes, family: size, direction: minimize, unit: B}
  - {name: store_fallback_plaintext_fraction, source: stdout_json, key: payload.store_fallback_plaintext_fraction, family: other, direction: none, unit: fraction}
  - {name: roundtrip_mismatches, source: stdout_json, key: payload.roundtrip_mismatches, family: correctness, direction: minimize, unit: count}
  - {name: portfolio_digest_sha256, source: stdout_json, key: payload.portfolio_digest_sha256, family: correctness, kind: string}
  - {name: ratio, source: derived, op: ratio, args: [artifact_bytes, input_bytes], family: size, direction: minimize}
  - {name: max_rss_bytes, source: resource, family: memory_peak, direction: minimize, unit: B}
  - {name: wall_s, source: resource, family: time_wall, direction: minimize, unit: s}
repetitions: 2
warmups: 0
seeds: {order: 20260917, bootstrap: 20260917, command: 20260917}
timing: false
order: randomized_blocks
raw_compression: gzip
platform:
  os: [linux]
  arch: [x86_64]
  emulated: false
  requires: [/root/eb-research/target/harness/release/ebr-codec, /root/eb-research/target/harness/release/ebr-planner]
  timeout_s: 172800
analysis:
  baseline: b-sq-balanced
  pairing: item
  report_metrics: [artifact_bytes, ratio, pcm_reconciliation_mismatches, roundtrip_mismatches]
