# ebr spec (schema ebr.spec.v1) -- Phase C-design pre-registration DRAFT, platform domain.
# Not executable as decision evidence until the gates in protocol.md are met; scripts named in
# `command` are specified in protocol.md (Commands) and are written before the first run.
schema: ebr.spec.v1
experiment_id: EXP-PLAT-012
status: pre-registration-draft
question: Which salvage and origin marker forms survive common Linux transports?
hypothesis: xattr markers are lost by at least half of transports; filename suffixes and manifests survive
  all.
decision_ids:
- DEC-PLT-027
- DEC-PLT-031
- DEC-MOD-035
- DEC-INT-034
- DEC-PLT-060
- DEC-CON-001
candidates:
- name: cp
  params:
    transport: cp
- name: cp-a
  params:
    transport: cp-a
- name: cp-preserve-xattr
  params:
    transport: cp-preserve-xattr
- name: mv-crossfs
  params:
    transport: mv-crossfs
- name: rsync-a
  params:
    transport: rsync-a
- name: rsync-aX
  params:
    transport: rsync-aX
- name: gnutar-default
  params:
    transport: gnutar-default
- name: gnutar-xattrs
  params:
    transport: gnutar-xattrs
- name: bsdtar
  params:
    transport: bsdtar
- name: zip-unzip
  params:
    transport: zip-unzip
- name: sevenzip
  params:
    transport: sevenzip
- name: git
  params:
    transport: git
command: bash /mnt/d/Projects/entrybound/entrybound/research/experiments/EXP-PLAT-012/scripts/marker_transports.sh
  --transport {transport} --fixture-from {item_id} --scratch {scratch}
shell: bash
corpus:
  generated:
  - item_id: plat012-fixture-tuning
    family: synthetic-trigger
    generator: zeros-v1
    bytes: 1
    seed: 20260917
  - item_id: plat012-fixture-validation
    family: synthetic-trigger
    generator: zeros-v1
    bytes: 1
    seed: 20260918
metrics:
- name: restore_fidelity_fraction
  source: stdout_json
  key: payload.restore_fidelity_fraction
  family: other
  direction: maximize
  unit: fraction
- name: observation_sha256
  source: stdout_json
  key: payload.observation_sha256
  family: correctness
  kind: string
repetitions: 2
warmups: 0
record_warmups: true
seeds:
  order: 20260917
  bootstrap: 20260917
  command: 20260917
timing: false
order: randomized_blocks
raw_compression: gzip
platform:
  os:
  - linux
  arch:
  - x86_64
  emulated: false
  requires:
  - rsync
  - tar
  - bsdtar
  - zip
  - unzip
  - 7z
  - git
  timeout_s: 3600
  scratch_root: /root/eb-research/scratch/platform
analysis:
  baseline: cp
  pairing: item
  report_metrics:
  - restore_fidelity_fraction
