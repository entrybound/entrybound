export const meta = {
  name: 'eb-research-phase-b1-infra-corpus',
  description: 'Entrybound research Phase B1: environment capture, runner/stats framework, versioned research corpus (20 families, splits), incumbent baseline tooling & capability matrix',
  phases: [
    { title: 'Framework', detail: 'env capture, runner/stats, corpus framework' },
    { title: 'Corpus', detail: 'family-group agents provision tuning/validation/held-out items' },
    { title: 'Assemble', detail: 'manifest, statistics, methodology, licenses' },
    { title: 'Critique', detail: 'corpus completeness critic, loop until dry' },
    { title: 'Baselines', detail: 'incumbent tooling, capability matrix, size/determinism pass' },
  ],
}

const REPO = 'D:/Projects/entrybound/entrybound'
const WREPO = '/mnt/d/Projects/entrybound/entrybound'
const DATA = '/root/eb-research'
const PY = 'C:/Python313/python.exe'

const RULES = `Shared rules for all Entrybound research-infrastructure agents:
- Repository: ${REPO} (Windows path) = ${WREPO} inside WSL. Branch dev, research baseline SHA 9e44608. Never run git commands that change state (no add/commit/checkout/stash/reset); the orchestrator commits.
- Large data lives OUTSIDE git in WSL ext4: ${DATA} (corpus at ${DATA}/corpus/<split>/<item_id>, held-out at ${DATA}/heldout/<item_id>, downloads cache at ${DATA}/cache, Python venv at ${DATA}/venv). Only scripts, declarative source lists, manifests, hashes, small raw results go in ${REPO}/research.
- Run Linux work in WSL Ubuntu 24.04 (root) by writing a script under ${REPO}/research/... and invoking: wsl.exe -d Ubuntu -- bash ${WREPO}/research/<path>.sh  (avoid complex inline quoting). Windows host: Windows 11 Pro, non-admin, Python ${PY}, Java 21, PowerShell 7. Docker Desktop (Linux containers, same WSL2 kernel) is available; ARM64 only via QEMU emulation (label as emulated). No GitHub Actions or hosted runners.
- Installed in WSL: GNU tar 1.35, bsdtar 3.7.2, 7-Zip 23.01 (7zz) + p7zip 16.02 (7z), zstd 1.5.5, xz 5.4.5, gzip 1.12, pigz, lz4, lzip, brotli, pixz, par2, hyperfine, cpio, squashfs-tools, xfsprogs, btrfs-progs, attr, acl, sqlite3, jq, python3.12, rustup stable 1.98.1 and nightly-2026-08-01, cargo-fuzz. You may apt-get install further packages from Ubuntu repos, pip install from PyPI into ${DATA}/venv, and download from reputable primary hosts (project release pages, kernel.org, python.org, GitHub releases of the upstream project, Wikimedia Commons, NASA, PyPI, crates.io, Maven Central, Docker Hub official images pinned by digest). Record every download URL, retrieval date (2026-09-12), size, and SHA-256.
- Do NOT collect timing measurements intended to support decisions in this phase: many agents are running concurrently, so the machine is not quiet. Byte counts, determinism checks, capability probes and smoke tests are allowed. Timing collection will run later in a quiet window via the runner's --timing guard.
- The held-out split must never be read by any experiment/tuning code. Provisioning may download/generate held-out items and compute fingerprints (hash, bytes, file count) only; content statistics for held-out items are deferred until a recorded design-freeze commit.
- Privacy: never use personal files from the Windows user profile or machine-specific logs; use public or generated data only.
- Every script must be rerunnable and idempotent (skip if fingerprint matches), and must fail loudly on hash mismatch.`

const RESULT = { type: 'object', properties: { files: { type: 'array', items: { type: 'string' } }, summary: { type: 'string' }, problems: { type: 'array', items: { type: 'string' } } }, required: ['files', 'summary'] }

phase('Framework')
const envJob = agent(`${RULES}

Task: environment fingerprinting.
Write ${REPO}/research/tools/env/capture_env.py (stdlib only, runs on both Windows Python 3.13 and WSL Python 3.12) that emits a JSON environment fingerprint: host OS/build, kernel, WSL/virtualization indicators, CPU model, physical/logical cores (on Windows also P-core/E-core split via Win32 APIs or wmic/PowerShell where possible), RAM, filesystem type of the working dirs, power scheme (Windows powercfg), rustc/cargo versions (rustup stable in the repo dir), git HEAD of the repo, Cargo.lock SHA-256, and versions of every archiver/compressor/tool on PATH relevant to baselines (tar, bsdtar, 7z, 7zz, zstd, xz, gzip, pigz, lz4, lzip, brotli, pixz, par2, zip/unzip, python, java, docker), plus Docker engine info. The fingerprint must have a stable env_id = SHA-256 of the canonical JSON of its stable fields (exclude timestamps/load).
Capture fingerprints for: Windows host, WSL Ubuntu, and a Docker container (ubuntu:24.04 pinned by digest; record digest). Store in ${REPO}/research/environment/<env_id>.json plus ${REPO}/research/environment/index.json mapping names -> env_id. Also write ${REPO}/research/environment/README.md explaining the fields and the CPU topology caveats (i9-14900HX hybrid P/E cores; WSL2 exposes 32 logical CPUs without P/E labels; laptop thermal behavior; Windows power scheme "Balanced" at capture time).
Return files and summary.`, { label: 'framework:env', phase: 'Framework', schema: RESULT })

const runnerJob = agent(`${RULES}

Task: experiment runner and statistics framework used by every later experiment.
Create a Python package at ${REPO}/research/tools/ebr/ (importable with PYTHONPATH=${WREPO}/research/tools) with a pinned requirements file ${REPO}/research/tools/requirements.txt (numpy, scipy, matplotlib, pandas, pyyaml, psutil — exact versions) installed into ${DATA}/venv, and pytest tests under ${REPO}/research/tools/tests. Modules:
1. ebr.spec: experiment spec schema (YAML or JSON) with fields experiment_id, question, hypothesis, decision_ids, candidates (named configs), corpus selector (item ids/families/splits; refuses split=heldout unless env EB_HELDOUT_UNLOCK equals the SHA recorded in ${REPO}/research/decisions/design-freeze.json, which does not exist yet), metrics, repetitions, warmups, seeds, timing (bool), platform requirements.
2. ebr.run: executes a command template per (candidate, item, repetition) with: warmup runs discarded; on Linux wrap with /usr/bin/time -v and parse wall/user/sys/max RSS/FS in/out; on Windows sample peak working set with psutil; optional CPU pinning (taskset on Linux, psutil cpu_affinity on Windows); scratch-space peak measurement by polling a scratch dir size; randomized but seeded run order (interleave candidates to decorrelate drift); a quiet-machine guard for timing=true runs that refuses to start (and marks samples invalid) if 1-minute load average or other-process CPU use exceeds a configurable threshold, re-checked between samples; records env_id from research/environment. Raw results append to ${REPO}/research/raw/<experiment_id>/<run_id>.jsonl (one row per sample, including command, exit code, stdout/stderr digests, metrics, timestamps, env_id, git SHA, corpus item fingerprint). Large raw artifacts may be gzip-compressed (.jsonl.gz).
3. ebr.stats: median, p90, p95, IQR, MAD, bootstrap CIs (seeded, >=10000 resamples) for medians and paired median ratios, noise rule helper (distinguishable only if CI excludes the practical-significance band), epsilon-Pareto dominance and frontier extraction, Kendall tau, weight-perturbation sensitivity helper (grid ±25/±50% and Dirichlet samples, fraction-of-perturbations-preserving-winner), geometric means.
4. ebr.normalize: raw -> ${REPO}/research/normalized/<experiment_id>/*.csv plus summary JSON; every table/plot generated by ebr.report with a command line recorded in the output file header, so no aggregate number is hand-entered.
5. ebr.report: markdown table and matplotlib figure generation (PNG + source CSV), figures under ${REPO}/research/reports/figures/<experiment_id>/.
6. CLI entrypoint: python -m ebr {run,normalize,report,verify-raw} with --spec.
Read ${REPO}/research/decision-method.md if it exists (it may still be in draft) and align thresholds handling (thresholds are read from a machine-readable ${REPO}/research/methods/thresholds.json that you create as a TEMPLATE with keys per metric family and value null + a note that decision-method.md is authoritative; do not invent values).
Include a smoke experiment spec at ${REPO}/research/experiments/EXP-SMOKE-000/spec.yaml that compresses a generated 1 MiB file with gzip in WSL for 3 reps (timing=false) and verify the whole pipeline end-to-end, including normalize/report. Run pytest. Return files and summary.`, { label: 'framework:runner', phase: 'Framework', schema: RESULT })

const corpusFramework = await agent(`${RULES}

Task: corpus framework (runs before the family agents, who will use your tools).
Create ${REPO}/research/corpus/tools/ with:
1. fingerprint.py: canonical tree fingerprint for a file or directory: walk without following symlinks; for every object record type (file/dir/symlink/hardlink-group/fifo/other), relative path as raw bytes hex, size, SHA-256 of content, mode, symlink target bytes hex, hardlink group (by inode within the item), xattr names+value hashes, sparse allocation (st_blocks) — and emit tree_sha256 = SHA-256 over sorted canonical lines. Also bytes, file/dir/symlink/hardlink counts, max depth, path length stats.
2. provision.py: reads declarative source definition files ${REPO}/research/corpus/sources/*.json, each item: item_id (kebab, globally unique), family (F01..F20, see below), split (tuning|validation|heldout), scale (small <=16MiB | medium <=512MiB | large <=8GiB), kind (download|generate|derive|docker-export|git-archive|build), real_or_generated (real|generated|derived-from-real), recipe (URLs with expected SHA-256 or "TOFU" on first retrieval then pinned; generator script path + seed + params; derivation steps), license {spdx_or_name, redistributable true/false, attribution, notes}, independence_group (items sharing upstream origin share a group; splits must not share groups within a family), description. It materializes into ${DATA}/corpus/<split>/<item_id> or ${DATA}/heldout/<item_id>, fingerprints it, and writes ${REPO}/research/corpus/fingerprints/<item_id>.json. Pinned hash mismatch fails. Supports --family/--group/--item filters and --jobs.
3. stats.py: per-item content statistics for tuning/validation only (refuse heldout without unlock): content-type distribution by magic sniffing (use libmagic via 'file --mime-type' in WSL), extension distribution, exact-duplicate file ratio, 4 KiB fixed-block duplicate ratio, byte-entropy histogram & mean Shannon entropy, zstd -3 and xz -6 compressibility (single-shot ratios as indicators only), metadata characteristics (symlinks, hardlinks, xattrs, ACLs, sparse files, mode variety, non-UTF-8 names).
4. assemble.py: builds ${REPO}/research/corpus/manifest.json (corpus_version "ebrc-2026.09-v1", item list with provenance/license/fingerprint refs and split/family/scale), ${REPO}/research/corpus/statistics.json (tuning+validation stats; heldout = fingerprint-only with note), a family x split x scale coverage table, and a manifest_sha256; and ${REPO}/research/corpus/heldout-lock.json containing the held-out item ids and tree hashes (the frozen held-out set).
5. A draft ${REPO}/research/corpus/methodology.md describing families, split discipline, independence groups, scale tiers, fingerprint algorithm, held-out access control, and threats (e.g. popular benchmark sets may have influenced upstream codec tuning).
Families (IDs are fixed): F01 source-code repositories; F02 build trees; F03 dependency/vendor trees; F04 many-small-file trees; F05 logs/text; F06 JSON/XML/CSV/structured text; F07 scientific/numeric arrays; F08 databases; F09 executable/binary objects; F10 highly redundant binaries; F11 already-compressed files; F12 JPEG/images; F13 other media; F14 archive-inside-archive; F15 sparse files; F16 VM/disk-like images; F17 duplicate trees; F18 near-duplicate/versioned trees; F19 metadata-heavy filesystem trees; F20 adversarial/high-entropy inputs.
Test the tools on a tiny generated item. Create ${DATA}/venv if missing (python3 -m venv). Return files and summary including the exact source-definition schema.`, { label: 'framework:corpus', phase: 'Framework', schema: RESULT })
log(`corpus framework: ${corpusFramework ? corpusFramework.summary.slice(0, 200) : 'FAILED'}`)

phase('Corpus')
const GROUPS = [
  { key: 'g1-code', families: 'F01 source-code repositories, F02 build trees, F03 dependency/vendor trees, F18 near-duplicate/versioned trees', hints: 'Use pinned upstream release tarballs or git archives at tags (different projects per split, e.g. tuning: zstd, ripgrep, curl; validation: CPython, redis; held-out: e.g. a Linux kernel subsystem subset, Go stdlib, PostgreSQL). Build trees: actually build pinned projects in WSL or pinned Docker images (e.g. cargo build of ripgrep; CPython or zstd cmake build dirs) and capture the build output directory. Vendor trees: cargo vendor of a pinned lockfile, npm ci of a pinned package-lock, pip install of pinned wheels into a target dir. Versioned: successive release series (e.g. zstd 1.5.0-1.5.6, SQLite amalgamation versions, CPython 3.12.x point releases) as multi-version trees, plus small edit-distance git history snapshots.' },
  { key: 'g2-generated', families: 'F04 many-small-file trees, F15 sparse files, F17 duplicate trees, F19 metadata-heavy filesystem trees, F20 adversarial/high-entropy inputs', hints: 'Mix real and generated. F04: real /usr/include and /usr/share/doc trees exported from pinned Docker images plus generated small-file trees with realistic size distributions. F15: generated sparse files with varied hole layouts (VM-like preallocated images, database preallocation, huge-hole/single-extent, many-small-extents) — verify holes with SEEK_HOLE. F17: exact duplicate trees (2x/8x copies, duplicated vendor dirs). F19: generated trees with deep nesting, many empty files/dirs, symlinks, hardlinks, xattrs (user.*), POSIX ACLs, varied modes/uids, long and Unicode names, plus a real /etc from a pinned Docker image. F20: /dev/urandom-seeded (use a seeded CSPRNG for reproducibility), AES-CTR ciphertext, CDC-adversarial patterns engineered to force min- or max-size chunks for the gear-norm-v1 chunker (read ${REPO}/docs/chunking-v1.md and ${REPO}/crates/entrybound/src/chunker.rs), highly compressible bomb-like inputs, and maximal-entropy files with sparse exact duplication. Generators go in ${REPO}/research/corpus/generators/ with fixed seeds.' },
  { key: 'g3-data', families: 'F05 logs/text, F06 JSON/XML/CSV/structured text, F07 scientific/numeric arrays, F08 databases', hints: 'F05: Loghub/LogPAI public log samples if license permits (record license), Project Gutenberg public-domain texts, enwik8 (Hutter prize), plus generated synthetic structured logs. F06: GH Archive hourly JSON, an OpenStreetMap small-region XML extract (ODbL), a small Wikimedia XML dump (CC BY-SA), public-domain government CSV (e.g. data.gov/BLS/Census). F07: Silesia sao/mr/x-ray (from the Silesia corpus), SDRBench scientific float arrays if downloadable, Unidata/NOAA NetCDF/HDF5 sample files, generated smooth float32/float64/int16 fields and time series with numpy seeds. F08: SQLite sample databases (e.g. Chinook), a generated SQLite TPC-H-like database, a PostgreSQL data directory produced by a pinned postgres Docker image after pgbench -i at a fixed scale, Silesia osdb.' },
  { key: 'g4-binary', families: 'F09 executable/binary objects, F10 highly redundant binaries, F11 already-compressed files, F14 archive-inside-archive, F16 VM/disk-like images', hints: 'F09: /usr/bin and /usr/lib from pinned Docker images (Ubuntu, Alpine, Fedora — different independence groups), Windows PE binaries from official redistributable releases (e.g. Python embeddable zip, Git for Windows portable), Silesia mozilla/ooffice/samba. F10: Rust/C debug builds with symbols, static libraries, linux-firmware subset, multiple builds of the same project. F11: .deb/.whl/.jar/.tar.gz/.xz/.zst release artifacts, PNG/JPEG excluded (F12), MP4 excluded (F13). F14: .jar/.whl/.nupkg/.apk-like zips, OOXML/ODF documents (generate with python libraries if needed), zip-of-zips, tar.gz inside tar, nested 3 levels. F16: raw ext4/FAT images built with mkfs + populated from Alpine minirootfs (pinned), a converted official cloud image (e.g. Alpine virt or Debian nocloud raw) if size permits, qcow2 vs raw variants.' },
  { key: 'g5-media', families: 'F12 JPEG/images, F13 other media', hints: 'F12: public-domain JPEGs from NASA image library and Wikimedia Commons (verify each license), JPEGs derived from lossless sources (Kodak suite PNGs) encoded by several encoders and modes (libjpeg-turbo baseline, mozjpeg, progressive, arithmetic-coded if available, 4:2:0/4:4:4, restart markers, EXIF-heavy) because JPEG reconstruction eligibility depends on producer; include CMYK/grayscale, truncated and corrupt JPEGs, JPEGs with trailing data. F13: PNG, WebP, GIF, TIFF, MP3/FLAC/Ogg from LibriVox or public domain, MP4/WebM (e.g. Blender open movie low-resolution files, CC BY), PDF (public-domain government PDFs). Different independence groups per split.' },
]

const COMMON_CORPUS = `Use the corpus framework in ${REPO}/research/corpus/tools/ (read its code and the framework summary below first). Write your source definitions to ${REPO}/research/corpus/sources/<your group key>.json and any generator/build scripts to ${REPO}/research/corpus/generators/<your group key>/. Then provision everything (tuning, validation, held-out) and fingerprint it.
Coverage requirements for EACH of your families: >=2 independent samples (distinct independence groups) in tuning, >=1 (prefer 2) in validation, >=2 in held-out, with no independence group shared across splits within a family; small/medium/large scales where meaningful (large only where the family realistically has large instances; total for your group should stay under ~12 GiB). Prefer real-world data; label generated data as such. Record license per item; mark non-redistributable items (only hashes committed). If a planned source is unreachable or license-incompatible, substitute and record why. Report per-family coverage (split x scale counts) in your summary and list any family you could not cover adequately in problems.

Framework summary: ${corpusFramework ? corpusFramework.summary : 'framework agent failed; read the tools directory directly'}`

const groupResults = await parallel(GROUPS.map(g => () => agent(`${RULES}\n\nTask: provision corpus family group ${g.key}: ${g.families}.\nSource hints (adapt as needed, verify licenses): ${g.hints}\n\n${COMMON_CORPUS}`, { label: `corpus:${g.key}`, phase: 'Corpus', schema: RESULT })))
groupResults.forEach((r, i) => log(`${GROUPS[i].key}: ${r ? r.summary.slice(0, 300) : 'FAILED'}`))

phase('Assemble')
const assembleCorpus = (round) => agent(`${RULES}

Task: corpus assembly (round ${round}). Run ${REPO}/research/corpus/tools/assemble.py and stats.py (tuning+validation only) over all source definitions and fingerprints; fix integration problems in the tools (not in upstream data). Produce/refresh ${REPO}/research/corpus/manifest.json, statistics.json, heldout-lock.json, a generated coverage table at ${REPO}/research/corpus/coverage.md (family x split x scale, independence groups, real vs generated), ${REPO}/research/corpus/licenses.md (generated from source definitions: item, license, redistributable, attribution), and finalize ${REPO}/research/corpus/methodology.md (keep generated numbers out of prose; reference coverage.md/statistics.json). Verify every fingerprint file matches the materialized data (spot-check 10% by recomputation). Return files, summary with totals (items, bytes, per split), and problems.`, { label: `corpus:assemble:r${round}`, phase: 'Assemble', schema: RESULT })
await assembleCorpus(0)

phase('Critique')
const CRIT = { type: 'object', properties: { gaps: { type: 'array', items: { type: 'object', properties: { family: { type: 'string' }, problem: { type: 'string' }, fix: { type: 'string' } }, required: ['family', 'problem', 'fix'] } }, summary: { type: 'string' } }, required: ['gaps', 'summary'] }
let r = 1
while (r <= 2) {
  const crit = await agent(`${RULES}

Task: adversarial corpus completeness critic (round ${r}). Read ${REPO}/research/corpus/manifest.json, coverage.md, statistics.json, licenses.md, sources/*.json. The corpus is INCOMPLETE if a major Entrybound use case has no representative workload. Entrybound use cases include: source releases, package publishing, CI artifacts, ML model/data distribution (large safetensors/numpy weights), scientific archives, software supply-chain verification, backups of home/project directories, legacy ZIP/tar/7z migration, container/VM images, media libraries, logs/telemetry archival, database dumps, cross-platform metadata-rich trees, encrypted private archives, adversarial inputs. Check: every family F01-F20 has >=2 independent tuning samples, >=1 validation, >=2 held-out, no shared independence group across splits, meaningful scale coverage, generated vs real balance, license recording, and that one workload is not standing in for a whole category. List concrete gaps with fixes (specific sources). Return gaps (empty if none) and a summary.`, { label: `corpus:critic:r${r}`, phase: 'Critique', schema: CRIT })
  if (!crit || crit.gaps.length === 0) { log(`corpus critic round ${r}: no gaps`); break }
  log(`corpus critic round ${r}: ${crit.gaps.length} gaps`)
  const byGroup = {}
  for (const gap of crit.gaps) {
    const g = GROUPS.find(x => x.families.includes(gap.family.slice(0, 3))) || GROUPS[1]
    ;(byGroup[g.key] = byGroup[g.key] || []).push(gap)
  }
  await parallel(Object.entries(byGroup).map(([gk, gaps]) => () => agent(`${RULES}\n\nTask: close corpus gaps for group ${gk}. Gaps:\n${gaps.map(x => `- ${x.family}: ${x.problem} -> ${x.fix}`).join('\n')}\n\n${COMMON_CORPUS}\nAppend to (do not remove existing items from) ${REPO}/research/corpus/sources/${gk}.json.`, { label: `corpus:fix:${gk}:r${r}`, phase: 'Critique', schema: RESULT })))
  await assembleCorpus(r)
  r++
}

phase('Baselines')
const baselines = await agent(`${RULES}

Task: incumbent baseline tooling, capability matrix, and a non-timing size/determinism pass.
1. ${REPO}/research/baselines/configs.json: pinned incumbent configurations, at minimum: ZIP/DEFLATE (Info-ZIP zip -6 and -9 if installed via apt 'zip'; 7zz -tzip -mx=9), tar+gzip (-6, -9; pigz -9 single-thread vs multi), tar+zstd (-3, -19, --ultra -22 --long=27; -T1 vs -T0), tar+xz (-6, -9e; pixz for indexed xz), 7z/LZMA2 (7zz -mx=5 and -mx=9, solid and -ms=off non-solid), tar+lz4, tar+brotli -q11, plus materially relevant containers discovered via ${REPO}/research/requirement-ledger.csv and the spec's incumbent list: squashfs (mksquashfs -comp zstd/xz), WIM (apt wimtools: wimcapture LZX/LZMS, solid), borgbackup and restic (dedup repositories), DwarFS (if an official static release binary is obtainable), zpaq (upper bound, if packaged), and zstd --patch-from where relevant for versioned trees. Record exact versions in ${REPO}/research/baselines/tool-versions.json (generated by a script).
2. ${REPO}/research/baselines/run_baselines.py using the ebr runner framework (${REPO}/research/tools/ebr) where it exists: for each config and corpus item (tuning+validation only), create the artifact, verify full round-trip (tree fingerprint equality where the format can represent the tree; otherwise record exactly what differed), record complete artifact bytes, determinism (two runs, byte-identical?), and support for single-member extraction (and the command used). Timing and memory fields must be collected only when invoked with --timing inside the quiet-machine guard; in THIS phase run with timing disabled over all tuning+validation items at small/medium scale and a subset of large items, writing raw rows to ${REPO}/research/raw/EXP-BASE-SIZE/ and normalized CSV to ${REPO}/research/normalized/EXP-BASE-SIZE/.
3. Capability probes: ${REPO}/research/baselines/probes/ generating fixtures with paths (non-UTF-8, Unicode), mtime/atime/ctime/birth precision, permissions, ownership, symlinks, hardlinks, POSIX ACLs, xattrs, sparse files, and checking what each incumbent config preserves after create+extract in WSL (as root, on ext4); plus encryption, authentication, random access, streaming creation/extraction, deterministic output, partial retrieval, and verification capability determined by probe where possible and otherwise by primary documentation citation (man page / upstream spec URL). Output ${REPO}/research/baselines/capability-matrix.csv (columns: system_config, capability, support {YES,NO,PARTIAL,N/A}, method {PROBE,DOC}, evidence_ref, notes) generated from probe results, and include Entrybound (ebound built from ${REPO} in WSL at a separate clone ${DATA}/src/entrybound checked out at the current HEAD, CARGO_TARGET_DIR=${DATA}/target/baseline) probed the same way.
4. Write ${REPO}/research/baselines/README.md describing configs, capability-matching rules for fair comparison (when a baseline lacks a capability, the comparison states it rather than excluding Entrybound's cost), and reproduction commands.
Return files, summary, problems.`, { label: 'baselines:tooling', phase: 'Baselines', schema: RESULT })

const fw = await Promise.all([envJob, runnerJob])
return { env: fw[0] && fw[0].summary, runner: fw[1] && fw[1].summary, corpusFramework: corpusFramework && corpusFramework.summary, groups: groupResults.map((g, i) => ({ key: GROUPS[i].key, summary: g && g.summary, problems: g && g.problems })), baselines: baselines && { summary: baselines.summary, problems: baselines.problems } }
