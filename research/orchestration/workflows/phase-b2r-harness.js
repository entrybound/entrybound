export const meta = {
  name: 'eb-research-phase-b2r-harness',
  description: 'Entrybound research Phase B2: default-off research-internals feature, Rust research harness workspace, network emulation harness, cross-host build + smoke validation, with checkpoint commits',
  phases: [
    { title: 'Internals', detail: 'cfg-gated research-internals feature with byte-identity proof' },
    { title: 'Harness', detail: 'workspace skeleton then per-family harness crates' },
    { title: 'Netem', detail: 'application-level network emulation proxy and range origin' },
    { title: 'Validate', detail: 'cross-host builds, determinism smoke, adversarial review' },
  ],
}

const REPO = 'D:/Projects/entrybound/entrybound'
const WREPO = '/mnt/d/Projects/entrybound/entrybound'
const DATA = '/root/eb-research'
const PY = 'C:/Python313/python.exe'
const CKPT = `${PY} ${REPO}/research/orchestration/checkpoint.py`

const RULES = `Shared rules for Entrybound research Phase B2 agents:
- Repo ${REPO} (Windows) = ${WREPO} (WSL). Branch dev. Read ${REPO}/research/PROGRESS.md first (environment facts, pinned toolchain, open integration items).
- Rust toolchain for ALL research builds is pinned to 1.98.1: Windows ${'`'}%USERPROFILE%\\.cargo\\bin\\cargo.exe +1.98.1${'`'}, WSL ${'`'}/root/.cargo/bin/cargo +1.98.1${'`'} (plain rustc in WSL scripts is Ubuntu 1.75 — never use it). Use a distinct CARGO_TARGET_DIR per agent to avoid build-lock contention: WSL ${DATA}/target/b2-<your-label>, Windows D:/eb-research/target/b2-<your-label>. Keep targets out of the repo.
- Run WSL work via scripts: wsl.exe -d Ubuntu -- bash ${WREPO}/research/<path>.sh. Data outside git in ${DATA}. Held-out corpus (${DATA}/heldout) must never be read.
- Do NOT collect decision-grade timing (other agents are running). Smoke timings are allowed if labeled non-decision-grade.
- Do not change production semantics. Production files under crates/ may only be changed by the Internals agent, only additively and only behind the default-off cargo feature "research-internals".
- Never run git state-changing commands directly. When your work is COMPLETE and VERIFIED, commit exactly your own paths with the serialized checkpoint tool (it also appends a PROGRESS.md change-log line and pushes to origin/dev):
  ${CKPT} --subject "<imperative subject <=72 chars>" --body "<what and why>" --note "<one-line progress note naming artifacts and verification>" <your paths...>
  (add --allow-outside only if you are the Internals agent staging crates/ paths). If the checkpoint fails with lock timeout, retry; if it fails with a git error, report it in problems and do not attempt manual git.
- Code style: match surrounding Rust idioms; workspace lints forbid unsafe_code — the harness must also forbid unsafe code.
- STORAGE: never consume C: drive space. All build targets, caches and data go on D: (Windows D:/eb-research/...) or inside WSL (whose disk now lives on D:). Before any heavy build or data generation run {PY} {REPO}/research/orchestration/disk_guard.py and stop if it fails.
- DOCKER IS UNAVAILABLE on this host right now (Docker Desktop startup is blocked by a stale AF_UNIX socket error 1920). Never start, restart, stop, or reset Docker Desktop and never use its factory reset; skip Docker-dependent steps and record them as deferred.`

const RESULT = { type: 'object', properties: { files: { type: 'array', items: { type: 'string' } }, commits: { type: 'array', items: { type: 'string' } }, summary: { type: 'string' }, problems: { type: 'array', items: { type: 'string' } } }, required: ['files', 'summary'] }

phase('Internals')
const internals = await agent(`${RULES}

RESUME NOTE: a previous attempt was interrupted by a usage limit. Its additive partial implementation (9 production files, 2340 inserted lines, no deletions) and a draft test are preserved at /research/harness/wip/research-internals-partial.patch and /research/harness/wip/research_internals.rs.partial (commit 745591b); the production crates are at HEAD. Start by reviewing that patch critically (git apply it only if it satisfies every constraint below; otherwise rework it), then finish, test, and prove byte identity. After your checkpoint, delete the two wip files in the same checkpoint (stage their removal paths).

Task: add a default-off cargo feature "research-internals" to crates/entrybound that exposes, READ-ONLY and WITHOUT changing any function body or behavior, the internal entry points a research harness needs. Read ${REPO}/research/audit/extract/code-compress-api-notes.md (section 2 lists what is private) and the other *-api-notes.md files in that directory.

Design constraints:
1. Declare in crates/entrybound/Cargo.toml: [features] research-internals = [] (default features unchanged).
2. Private items are visible to child modules, so add cfg-gated child modules, e.g. in planner.rs: #[cfg(feature = "research-internals")] pub mod research { ... } containing thin pub wrapper functions/types that call the private functions (candidate enumeration, complete candidate cost, qualification rules, cohort selection, dictionary training, lookback prefix sizing, JPEG region candidates), and likewise in codec.rs (plan constructors, encode/decode payload variants, dictionary training/validation, decode requirement aggregation, supported level/lookback/LZMA2 constant sets), transform.rs (forward/inverse pipelines, step constructors), reconstruction.rs (try_forward/verified_forward/inverse/validate, limits), jpeg_reconstruction.rs (verified_forward/inverse/region identity/limits), similarity.rs (fingerprint, intersection_count), and ecf (encoded record length helpers, physical prefix construction). Then in lib.rs add #[cfg(feature = "research-internals")] pub mod research { pub use crate::planner::research as planner; ... } re-exporting them.
3. Add a candidate-enumeration API suitable for planner-optimality research: given a chunk (or cohort) and a profile/planner version, return EVERY candidate the production planner considers with its predicted cost components (payload bytes, record bytes, side data, qualification result and exclusion reason) and which one the production rule selects — implemented by calling the same private functions the planner uses, so the harness can verify its reconstruction equals the planner's actual choice. Do not add hooks inside production function bodies.
4. Tests: add crates/entrybound/tests/research_internals.rs with #![cfg(feature = "research-internals")] asserting (a) for a set of generated inputs (text, binary, zeros, random, DEFLATE stream, JPEG built with dev-dependency image/jpeg-encoder) the candidate enumeration's selected plan equals the plan the real plan_archive_v4/v5/v6 chose; (b) codec encode/decode round-trip via the research API equals the archive's stored payload bytes.
5. Byte-identity proof: write ${REPO}/research/harness/internals-identity-check.sh (WSL) and a matching PowerShell script that build the CLI with and without --features entrybound/research-internals and pack a fixed generated tree under all four profiles in INDEXED and STREAM layouts and compare SHA-256 of outputs (must be identical), plus run cargo test -p entrybound -p entrybound-cli without the feature and cargo test -p entrybound --features research-internals, plus cargo clippy --all-targets -- -D warnings both ways and cargo fmt --check. Record results to ${REPO}/research/harness/internals-identity-check.md (generated by the script, with command lines and hashes).
6. Document the feature in ${REPO}/research/harness/research-internals.md: purpose, exposed items, the no-behavior-change rule, and that a Decision Ledger entry is required before any research API is promoted to a stable production API.
Verify all of the above on WSL (primary) and at least cargo test with and without the feature on Windows. Then checkpoint (with --allow-outside) staging crates/entrybound/Cargo.toml, the modified crates/entrybound/src files, crates/entrybound/tests/research_internals.rs, and research/harness/{internals-identity-check.sh,internals-identity-check.ps1,internals-identity-check.md,research-internals.md}. Return files, commits, summary, problems.`, { label: 'internals:feature', phase: 'Internals', schema: RESULT })
log(`internals: ${internals ? internals.summary.slice(0, 300) : 'FAILED'}`)

phase('Harness')
const skeleton = await agent(`${RULES}

Task: create the research harness Cargo workspace skeleton at ${REPO}/research/harness (its OWN [workspace], not a member of the production workspace; verify that production cargo test --workspace from the repo root is unaffected).
Members (create each as a compiling stub with a README stating its purpose):
- crates/ebr-common: shared library — corpus manifest reader (${REPO}/research/corpus/manifest.json when present; must call a held-out refusal equivalent to research/corpus/tools/corpuslib.py assert_not_heldout: refuse any path under ${DATA}/heldout unless EB_HELDOUT_UNLOCK matches research/decisions/design-freeze.json), tree walking of corpus items into in-memory or streaming inputs, JSON-lines result writer with schema version, experiment/run/env id fields (read research/environment/index.json), process self-measurement helpers (peak RSS via getrusage on Linux / GetProcessMemoryInfo through a safe crate on Windows), monotonic timing helpers, SHA-256 helpers.
- crates/ebr-pack: end-to-end archive measurements using public production APIs (pack/plan/encode/open/verify/unpack/random read) with complete byte accounting.
- crates/ebr-chunk: chunking/dedup research (production gear-norm-v1 via entrybound::chunker plus research-only alternative CDC implementations).
- crates/ebr-codec: codec/transform/reconstruction research (production internals via entrybound research-internals feature plus candidate external codecs).
- crates/ebr-planner: planner candidate traces, exhaustive search, regret.
- crates/ebr-access: random/remote access and streaming/memory measurements.
- crates/ebr-netem: network emulation proxy and range origin (built by a separate agent).
Dependency on production: entrybound = { path = "../../crates/entrybound", features = ["research-internals"] } (the feature is being added concurrently by the Internals agent; ${internals ? 'it has completed: ' + internals.summary.slice(0, 400) : 'it FAILED — use public APIs only and leave TODO markers listed in problems'}).
Pin third-party crate versions exactly (=x.y.z) and commit the harness Cargo.lock. Add ${REPO}/research/harness/README.md (layout, build commands for WSL and Windows with pinned toolchain, target-dir conventions, how binaries plug into ebr runner specs via command templates, output schema), and build scripts research/harness/build.sh (WSL, CARGO_TARGET_DIR=${DATA}/target/harness default) and build.ps1 (Windows). Build the skeleton on WSL. Then checkpoint research/harness (excluding target). Return files, commits, summary, problems.`, { label: 'harness:skeleton', phase: 'Harness', schema: RESULT, model: 'sonnet' })
log(`skeleton: ${skeleton ? skeleton.summary.slice(0, 300) : 'FAILED'}`)

const FAMILIES = [
  { key: 'pack', crate: 'ebr-pack', task: `Binaries: ebr-pack (pack a directory or corpus item with --profile fast|balanced|dense|extreme, --layout indexed|stream, --stream-window, optional --encrypt with generated test recipient/password, --deterministic-check N to repeat and hash-compare), ebr-unpack (full verify+extract to a scratch dir with peak scratch measurement), ebr-verify, ebr-inspect-bytes. Output one JSON line with: complete artifact bytes and an EXACT section-level byte accounting (preamble/descriptor, manifest/entry records, metadata records, index bytes, dictionary bytes, chunk frame headers, chunk payload bytes by codec/transform, reconstruction side-data, chunk-group records, padding bytes, signatures, footer) derived by walking the encoded container with production parsing APIs; cross-check the sum equals file size (fail loudly otherwise). Also logical bytes, entry/chunk/unique-chunk/dictionary/group counts, per-codec stored/logical bytes, planner id, internal wall-clock per phase (capture, chunking, planning, encoding, writing) and peak RSS. Add tests on generated trees.` },
  { key: 'chunk', crate: 'ebr-chunk', task: `Binaries: ebr-chunk. Implement research-only CDC algorithms alongside production gear-norm-v1 (called through entrybound::chunker with arbitrary min/target/max parameters): fixed-size; FastCDC-2016 and FastCDC-2020 (normalized chunking with the published masks; you may cross-check against the 'fastcdc' crate pinned exactly as a reference implementation in tests); Rabin fingerprint CDC (64-bit polynomial, 48-byte window); BuzHash (cyclic polynomial, 64-byte window); AE (asymmetric extremum); RAM (rapid asymmetric maximum); and a byte-content-agnostic TTTD variant if tractable — each with explicit parameterization and a stable algorithm id. Cite the paper for each algorithm in a module doc comment. Subcommands: boundaries (emit chunk size histogram/quantiles, count, throughput sample), dedup (over a corpus item or multiple versions: unique chunk bytes, dedup ratio, estimated index/reference overhead using production cost constants), shift-stability (apply seeded insertions/deletions/overwrites of given sizes at random offsets and measure fraction of preserved boundaries and changed bytes-to-rewrite), and random-access amplification (bytes that must be read to access a random logical range). Tests: property tests for boundary determinism, min/max enforcement, and equality of gear-norm-v1 results with production chunk_ranges.` },
  { key: 'codec', crate: 'ebr-codec', task: `Binaries: ebr-codec. Using entrybound::research (research-internals) evaluate production codecs/plans exactly (STORE, Zstandard levels and dictionary/prefix modes, LZ4, raw LZMA2 presets, delta8/byte-shuffle pipelines, deflate-reconstruct, jpeg-jxl-reconstruct) per chunk or per object: stored bytes, encode/decode wall time sample, decoder working-set declaration, and round-trip equality. Also evaluate candidate external codecs not in production, pinned exactly: brotli (pure-Rust 'brotli' crate), bzip2 (already a dependency), DEFLATE/zlib levels (flate2), xz with BCJ x86/ARM64 filters (lzma-rust2 or liblzma bindings if safe), PPMd if a maintained safe crate exists (else record unavailability), LZ4 HC levels, Zstandard long-distance matching variants — each as a research-only candidate with explicit parameters; and structural transform candidates beyond production (BCJ x86/ARM64/RISC-V via safe implementations, float/integer delta-of-delta, byte-plane splitting) implemented in the harness. Subcommands: per-chunk matrix (candidate x chunk), per-object matrix, transform false-positive analysis (transform applied where it loses bytes). Tests: round-trip for every candidate on generated inputs; production-plan byte equality with archive payloads.` },
  { key: 'planner', crate: 'ebr-planner', task: `Binaries: ebr-planner. Using entrybound::research candidate enumeration, produce per-chunk/per-cohort traces: every candidate considered, predicted cost, actual complete cost (encode it and compute exact record+payload+side-data bytes), winner, second best, delta, exclusion reason. Implement (a) exhaustive search over the full candidate cross-product for tractable samples (chunk-level codec x transform x level x dictionary x lookback, bounded by explicit caps and recorded), (b) regret = actual chosen complete cost - optimal complete cost, (c) research-only alternative selection strategies: greedy with pruning, dynamic programming over group/lookback decisions, and a deterministic data-driven selector (e.g. decision tree trained on tuning traces with integer-only features and frozen thresholds) — all deterministic. Output JSONL traces and a CSV of candidate regret. Tests: trace reconstruction selects exactly what plan_archive_v6 selected on generated inputs; exhaustive search never returns a cost above the production choice.` },
  { key: 'access', crate: 'ebr-access', task: `Binaries: ebr-access. Measure INDEXED random access through entrybound public APIs (open_indexed_random with Memory/LocalFile/HttpRange sources and RandomAccessPolicy knobs: coalesce_gap_bytes, max_dependency_chunks, etc.): first-entry latency (time to first plaintext byte of the first entry), random-entry latency over a seeded sample of entries, random byte-range reads within large entries, bytes fetched, range request count, dependency chunk count, verification flags; eager full consumption; and STREAM sequential verify/list/unpack with peak RSS and scratch. Include a streaming memory probe mode that runs pack/unpack/verify/repack/export/publish operations over a synthetic streaming input generator of configurable size (e.g. 1-64 GiB without storing it) where APIs allow, reporting peak RSS and scratch at intervals to detect scaling with total plaintext. Tests on generated trees.` },
]
const familyResults = await parallel(FAMILIES.map(f => () => agent(`${RULES}

Task: implement the ${f.crate} crate in the research harness workspace ${REPO}/research/harness/crates/${f.crate} (only edit that crate directory; if you must add a workspace dependency, edit research/harness/Cargo.toml minimally and re-read it immediately before editing because other agents edit it concurrently).
Skeleton status: ${skeleton ? skeleton.summary.slice(0, 600) : 'skeleton agent failed; create what you need inside your crate dir'}
Internals status: ${internals ? internals.summary.slice(0, 600) : 'research-internals feature unavailable; use public APIs only'}
${f.task}
Build and test in WSL with CARGO_TARGET_DIR=${DATA}/target/b2-${f.key} and pinned toolchain; also confirm the crate builds on Windows. Document usage in the crate README with example ebr runner spec command templates. Then checkpoint research/harness/crates/${f.crate} (plus research/harness/Cargo.toml and Cargo.lock only if you changed them). Return files, commits, summary, problems.`, { label: `harness:${f.key}`, phase: 'Harness', schema: RESULT, model: 'sonnet' })))
familyResults.forEach((r, i) => log(`harness ${FAMILIES[i].key}: ${r ? 'ok' : 'FAILED'}`))

phase('Netem')
const netem = await agent(`${RULES}

Task: application-level network emulation for remote-access research (sch_netem is unavailable in WSL2 and Docker), in ${REPO}/research/harness/crates/ebr-netem (only edit that crate dir; edit the workspace Cargo.toml minimally with a re-read first).
Build two binaries (tokio + hyper or axum, pinned exactly, no unsafe):
1. ebr-origin: static HTTP/1.1 (and HTTP/2 if practical) file server for .eb archives with strong ETag, exact 206 single-range responses, optional multi-range (multipart/byteranges) support toggle, configurable object-store-like behaviors: max ranges per request, 200-full-body fallback toggle (to verify clients refuse it), weak/missing ETag toggle, revision change mid-session toggle, request logging (JSONL: timestamp, method, range, status, bytes sent).
2. ebr-netem-proxy: reverse proxy between client and origin applying per-connection RTT (added one-way delay on request and response first byte), bandwidth token-bucket shaping in each direction, jitter distribution, connection setup delay (TCP+TLS handshake RTT multiples), a loss model emulated as stochastic transfer stalls of RTO duration with seeded RNG (documented as an approximation of packet loss), max concurrent connections, and a CDN cache layer mode (warm/cold cache of range responses with configurable cache size and eviction, counting cache hits vs origin fetches). Log per-request bytes and timing.
3. Calibration: a test binary/script that measures the emulator's achieved RTT and throughput against configured values at several settings (e.g. RTT 1/20/100/300 ms; bandwidth 1/10/100/1000 Mbit/s) and writes ${REPO}/research/harness/crates/ebr-netem/calibration.md generated from raw results (raw JSONL under ${REPO}/research/raw/EXP-NETEM-CAL/). Label calibration timing as provisional (machine not quiet) and note it must be re-run in the quiet window.
4. Document threats to validity (application-level emulation vs kernel TCP dynamics, congestion control, HOL blocking) in the crate README.
Build/test in WSL with CARGO_TARGET_DIR=${DATA}/target/b2-netem. Then checkpoint the crate dir and raw calibration results. Return files, commits, summary, problems.`, { label: 'netem:proxy', phase: 'Netem', schema: RESULT, model: 'sonnet' })

phase('Validate')
const validate = await agent(`${RULES}

Task: integration validation of the Phase B2 research harness.
1. Build the whole harness workspace on WSL (CARGO_TARGET_DIR=${DATA}/target/harness) and Windows (D:/eb-research/target/harness) with the pinned toolchain; run all harness tests on WSL; fix integration breakages minimally (you may edit harness crates; do not touch production crates).
2. Re-run research/harness/internals-identity-check.sh and confirm production archive bytes are identical with and without research-internals.
3. Cross-host determinism smoke: pack the same generated tree (seeded, produced by a checked-in generator) with ebr-pack under all four profiles and both layouts on WSL x86-64, Windows x86-64, (emulated linux/arm64 via Docker is DEFERRED because Docker Desktop is unavailable on this host; record it as deferred in the summary table). Record hashes of artifacts and native identities (LAI/PCR/AUX/PCI) to ${REPO}/research/raw/EXP-DET-SMOKE/ and a generated summary table ${REPO}/research/normalized/EXP-DET-SMOKE/summary.csv. Any mismatch is a finding: report it precisely (do not fix production code).
4. Wire the harness into the ebr runner: add example runner specs under ${REPO}/research/experiments/EXP-HARNESS-SMOKE-*/spec.yaml that run one binary from each crate on a tiny generated item (timing=false) and complete run->normalize->report.
5. Update ${REPO}/research/harness/README.md with verified commands and known limitations.
Checkpoint your paths (harness fixes, raw/normalized EXP-DET-SMOKE, experiments EXP-HARNESS-SMOKE-*, and their normalized outputs/reports). Return files, commits, summary, problems (list every failing component).`, { label: 'validate:integration', phase: 'Validate', schema: RESULT, model: 'sonnet' })

const review = await agent(`${RULES}

Task: adversarial review of the research harness for measurement validity BEFORE it is used for decisions. Read the harness crates, ${REPO}/research/harness/*.md, and the research-internals feature diff in crates/entrybound (git log/diff of the research-internals checkpoint commit -- crates/). Check: (a) the feature truly cannot change default builds (cfg placement, no altered bodies, no changed visibility of default-build items, Cargo feature unification pitfalls e.g. the CLI or dev-dependencies enabling it); (b) byte accounting is exact and independently cross-checked; (c) alternative CDC implementations match their papers (parameters/masks) and are not strawmen; (d) external codec candidates are configured fairly (levels, window sizes, threads=1) and their memory/decoder complexity is measurable; (e) exhaustive planner search is genuinely exhaustive within stated caps and regret uses complete cost; (f) timing/RSS instrumentation is not dominated by harness overhead (e.g. in-memory corpus loading counted in pack time), and measurement of peak scratch is real; (g) network emulator semantics vs real TCP; (h) held-out guard cannot be bypassed accidentally. Write findings with severity and required fixes to ${REPO}/research/harness/harness-review-round1.md, apply fixes for every HIGH/MEDIUM finding that is a harness defect (re-run affected tests), and record dispositions in the same file. Checkpoint the review file and your fixes. Return files, commits, summary, problems.`, { label: 'validate:adversarial-review', phase: 'Validate', schema: RESULT })

return { internals: internals && internals.summary, skeleton: skeleton && skeleton.summary, families: familyResults.map((r, i) => ({ key: FAMILIES[i].key, summary: r && r.summary, problems: r && r.problems, commits: r && r.commits })), netem: netem && { summary: netem.summary, problems: netem.problems }, validate: validate && { summary: validate.summary, problems: validate.problems }, review: review && { summary: review.summary, problems: review.problems } }
