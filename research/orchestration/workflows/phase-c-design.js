export const meta = {
  name: 'eb-research-phase-c-design',
  description: 'Entrybound Phase C-design: pre-register experiment protocols/specs for every program section and decision, adversarial review, revision, pre-registration commit',
  phases: [
    { title: 'Design', detail: '15 domain design agents write protocols + ebr specs + index fragments' },
    { title: 'Index', detail: 'deterministic experiment index and decision coverage check' },
    { title: 'Review', detail: 'cross-domain adversarial design review' },
    { title: 'Revise', detail: 'resolve findings and commit the pre-registration record' },
  ],
}

const REPO = 'D:/Projects/entrybound/entrybound'
const WREPO = '/mnt/d/Projects/entrybound/entrybound'
const PY = 'C:/Python313/python.exe'
const CKPT = `${PY} ${REPO}/research/orchestration/checkpoint.py`

const RULES = `Shared rules (Entrybound research, Phase C-design = experiment pre-registration):
- Read first: ${REPO}/research/PROGRESS.md; ${REPO}/research/decision-method.md (authoritative: §3 thresholds, statistical protocol, §5 split discipline, §10 experiment record template, cost accounting); ${REPO}/research/archetypal-objective.md (hard constraints HC-*, dimensions OD-*/metrics M*); ${REPO}/research/methods/thresholds.json; ${REPO}/research/harness/README.md and research/harness/harness-review-round1.md (binding use constraints); ${REPO}/research/baselines/README.md; ${REPO}/research/corpus/coverage.md and manifest.json (tuning/validation items only); ${REPO}/research/findings/*.md.
- Query ${REPO}/research/decision-ledger.jsonl with scripts (filter by program_sections/cluster); never load it whole.
- You DESIGN experiments; you do not run them (at most a <=2 minute feasibility smoke on a tiny generated input, clearly labeled non-decision-grade). No held-out access: corpus selectors may name only tuning/validation items; held-out evaluation is Phase D and is designed only as a placeholder referencing decision-method.md §5.
- Environment realities: Windows 11 host (non-admin, NTFS), WSL2 Ubuntu 24.04 (root; ext4, loop-device XFS/btrfs), Docker UNAVAILABLE, macOS/APFS/native ARM64 PLATFORM_BLOCKED, no human participants, no external crypto reviewers, network emulation only application-level (ebr-netem, no slow start). Where evidence cannot be produced here, design the experiment anyway, mark it BLOCKED with the exact missing platform/access, so remaining-unknowns can cite it.
- Every experiment gets a stable ID EXP-<DOMAIN>-<NNN> and a directory research/experiments/<ID>/ containing protocol.md (the §10 template: question, hypothesis with falsification condition, decision_ids informed, candidates incl. status quo and exclusions with reasons, corpus selectors, environment, commands (harness binaries/ebr runner/scripts to be written, named precisely), seeds, warmup, measurement method and metrics (canonical metric names from decision-method Appendix A / thresholds.json keys), replication count and adaptive rule, normalization, statistical analysis, practical-significance thresholds (reference, never restate differently), sensitivity analysis, expected negative results worth recording, threats to validity, platform requirements (Linux/Windows/macOS/x86-64/ARM64/network/privileged/large storage/external review), estimated compute hours and disk, estimated agent effort (none/scripted/judgment)) and, when runner-driven, spec.yaml valid under ebr (validate with: wsl.exe -d Ubuntu -- bash -lc 'cd ${WREPO} && PYTHONPATH=research/tools /root/eb-research/venv/bin/python -m ebr validate --spec research/experiments/<ID>/spec.yaml').
- Prefer compute-heavy scripted experiments that need no agent tokens to execute; group many decisions into shared experiments where one measurement settles several.
- Write your domain's index fragment research/experiments/_index/<domain>.jsonl: one line per experiment {experiment_id, domain, title, program_sections, decision_ids, status (READY|BLOCKED|NEEDS_TOOLING), blocked_reason, platforms, compute_hours_estimate, disk_gb_estimate, requires_timing (bool), tooling_to_build}.
- Also list every decision in your program sections that NO experiment in your design addresses, with the reason (e.g. purely analytical/literature, external review, human participants) in research/experiments/_index/<domain>-uncovered.jsonl {decision_id, reason, evidence_route}.
- Never run state-changing git commands directly. Checkpoint your domain directories and fragments when complete via: ${CKPT} --subject "<<=72 chars>" --body "<what/why>" --note "<one-line>" -- <paths...>.`

const DOMAINS = [
  { key: 'chunking', sections: '8.1, 8.2', focus: 'CDC algorithm and parameter sweeps (gear-norm-v1 vs fixed, FastCDC-2016/2020, Rabin, BuzHash, AE, RAM, TTTD) per category and aggregate; dedup scopes (none/whole-file/exact-chunk/archive-wide/windowed); boundary stability under insertion; update locality; index overhead; random-access amplification; encrypted creation interaction; Pareto frontiers.' },
  { key: 'crossfile', sections: '8.3, 8.4, 8.5', focus: 'similarity clustering (bottom-k vs alternatives), dictionary policy sweeps charging dictionary bytes and decoder memory, chunk-group lookback 0/1/2/4/8+ with first-byte latency, access amplification, dependency bytes, parallelism loss, corruption propagation.' },
  { key: 'codecs', sections: '8.6, 8.7', focus: 'codec portfolio (implemented + external candidates via ebr-codec) with ratio/speed/memory/decoder complexity/dependency quality/spec availability/independent implementability; structural transforms (delta, shuffle, BCJ family, numeric) with false-positive cost.' },
  { key: 'reconstruction', sections: '8.8', focus: 'DEFLATE and JPEG (baseline/progressive) reconstruction prevalence, exact-roundtrip success, side-data, net savings, encode/decode/verify cost, implementation burden; other targets only if corpus prevalence justifies.' },
  { key: 'planner', sections: '9, 10', focus: 'planner optimality via ebr-planner exhaustive search and regret distributions by workload class; heuristic/DP/pruning/data-driven deterministic selectors; profile objective fit on tuning, validation checks, weight-sensitivity stability; whether four profiles and balanced default are defensible; per-profile parameters.' },
  { key: 'container', sections: '11', focus: 'manifest representations (current/compressed/segmented/tree), Index alternatives, Merkle outboard layouts and proofs at very large entry counts (generated million-entry trees), remote metadata transfer, parser memory, startup latency, format-revision justification; default resource budgets; magic-number identification check.' },
  { key: 'remote', sections: '12', focus: 'remote/random access via ebr-access + ebr-netem: RTT/bandwidth/loss/CDN cache/object-store behaviors; range coalescing thresholds, concurrency, cache sizes, eager vs lazy, proof granularity, metadata fetch strategy; bytes and request counts; slow-start sensitivity (harness review R1-14).' },
  { key: 'scale', sections: '13, 14', focus: 'streaming/memory/scalability beyond RAM (pack, STREAM/INDEXED creation, unpack, import, encryption, export, repack, publish) with cgroup/process limits in WSL or generated streaming inputs; which operations scale with plaintext; bounded staging; deterministic parallelism prototypes (hashing, chunk analysis, candidate evaluation, compression, verification) with repeated runs across worker counts requiring byte identity.' },
  { key: 'platform', sections: '15, 16, 17, 18, 19, 20, 21', focus: 'cross-platform fidelity and native path behavior on Windows NTFS (non-admin) and Linux ext4/XFS/btrfs (loop devices), streams/forks, special files, ownership/identity, origin markers, safe-Rust API feasibility; include findings F-0001/F-0002 follow-ups (feature tier for AUX metadata; LAI unobservable semantics; timestamp precision); macOS/APFS/ReFS experiments designed but BLOCKED.' },
  { key: 'crypto', sections: '22.1, 22.2, 22.3, 22.4, 22.5', focus: 'primary-source primitive review protocol; reproduction of CDC boundary leakage attacks against public CDC, secret Gear, PHTE/AES with leakage metrics, CPU, boundary stability, compression effect; padding privacy vs overhead modeling; signature/trust semantics test matrix; feature-composition matrix tests; external-review dossier assembly (EXPERT_REVIEW_REQUIRED items).' },
  { key: 'integrity', sections: '23, 24', focus: 'sidecar and incremental scenarios (missing/renamed originals, remote locators, base chains, cycles) with verification cost and attack surface; controlled corruption campaigns (bit flips, missing chunks, truncation, burst loss, footer/index/metadata/dependency corruption) measuring localization and blast radius; external parity (par2 and alternatives) overhead and repair success; verify/salvage/repair semantics.' },
  { key: 'legacy', sections: '25, 26, 27', focus: 'differential testing across ZIP runtimes, GNU tar, bsdtar/libarchive, Python tarfile, 7-Zip with generated primitive cases and recorded outcomes; adapter priority scoring (OCI, NAR, RAR5, cpio, ar, WIM, squashfs, lzip, LZ4 frame, others); export fidelity of tar/pax/GNU/star/ZIP extras across consuming runtimes; baseline probe findings (symlink refusal in tar export, non-UTF-8 and ACL-mask refusals).' },
  { key: 'conformance', sections: '28, 29, 30', focus: 'independent minimal reader implemented from the written spec only (language choice to maximize independence) with ambiguity log and production differential; versioned native conformance corpus with classes and resource ceilings; persistent and differential fuzzing (cargo-fuzz nightly in WSL) with minimization and promotion to regressions.' },
  { key: 'ecosystem', sections: '31, 32, 33', focus: 'dependency audit and classification (baseline reader/extended decoder/writer only/adapter only/test-research only) with measurable longevity signals; realistic adoption workflows with command/option counts and failure modes; simulated first-use CLI evaluations by agents not shown internals (clearly labeled simulated; human participants BLOCKED).' },
  { key: 'evaluation', sections: '6, 7, 34, 35', focus: 'incumbent baseline completion (size pass continuation, timing in quiet windows with production-built ebound per harness review), whole-system measurement framework gaps (e.g. harness review R1-16 RSS accounting), stable-v1 boundary scoring method, and the final held-out evaluation design including the sealed supplementary held-out set required by method review finding G01 (selection procedure fixed now, executed after design freeze).' },
]

phase('Design')
const designs = await parallel(DOMAINS.map(d => () => agent(`${RULES}

Your domain: ${d.key} (program sections ${d.sections}). Focus: ${d.focus}
Design the complete, minimal-but-sufficient set of experiments that together can settle (or precisely block) every decision in these sections, following decision-method.md. Use research/experiments/_index/${d.key}.jsonl and ${d.key}-uncovered.jsonl for your index fragments. Return {experiments: [ids], ready, blocked, needs_tooling, uncovered_decisions, commit, summary}.`,
  { label: `design:${d.key}`, phase: 'Design', schema: { type: 'object', properties: { experiments: { type: 'array', items: { type: 'string' } }, ready: { type: 'integer' }, blocked: { type: 'integer' }, needs_tooling: { type: 'integer' }, uncovered_decisions: { type: 'integer' }, commit: { type: 'string' }, summary: { type: 'string' } }, required: ['experiments', 'summary'] } })))
designs.forEach((r, i) => log(`${DOMAINS[i].key}: ${r ? `${r.experiments.length} experiments (ready ${r.ready}, blocked ${r.blocked}, tooling ${r.needs_tooling}), uncovered ${r.uncovered_decisions}` : 'FAILED'}`))
const failed = DOMAINS.filter((d, i) => !designs[i]).map(d => d.key)
if (failed.length) { log(`STOP: failed design domains ${failed.join(', ')} — rerun before indexing`); return { failed } }

phase('Index')
const index = await agent(`Write and run a deterministic stdlib-only tool ${REPO}/research/tools/experiments/build_index.py that merges research/experiments/_index/*.jsonl into research/experiments/index.csv and research/experiments/decision-coverage.csv (one row per decision in research/decision-ledger.jsonl: decision_id, program_sections, experiment_ids, uncovered_reason, evidence_route), validates that every experiment directory has protocol.md, every runner-driven experiment has a spec.yaml, every decision_id referenced exists, and every decision is either covered by >=1 experiment or has an explicit uncovered reason. Report counts (experiments by status/domain, decisions covered/uncovered by reason, total compute hours and disk). Checkpoint via ${CKPT} --subject "research: build experiment index and decision coverage" --body "<counts>" -- research/tools/experiments research/experiments/index.csv research/experiments/decision-coverage.csv. Return a summary with the counts and any validation errors.`, { label: 'index:build', phase: 'Index', model: 'sonnet' })
log(`index: ${index ? String(index).slice(0, 400) : 'FAILED'}`)

phase('Review')
const review = await agent(`${RULES}

Adversarially review the whole experiment pre-registration: research/experiments/index.csv, decision-coverage.csv, and every protocol.md/spec.yaml (sample deeply where needed, but check every experiment's metadata). Find: decisions with no credible evidence route; experiments that cannot distinguish candidates at the pre-registered thresholds (underpowered, noise-dominated, wrong metric); unfair incumbent comparisons (capability mismatch); tuning/held-out leakage; confounds (hybrid cores, WSL2, Defender, harness-build inlining per harness review, application-level netem); missing negative controls or ablations the program requires (every major physical optimization needs an ablation); cost-blind designs; experiments whose conclusions would rely on INFERRED rather than measured evidence; platform claims not tied to the right platform; missing complete-cost accounting in planner work; crypto conclusions that bypass external review. Write research/experiments/design-review-round1.md with findings (severity, experiment/decision IDs, required fix) and checkpoint it. Return a summary with counts by severity.`, { label: 'review:design', phase: 'Review' })

phase('Revise')
const revise = await agent(`${RULES}

Resolve every BLOCKER/HIGH/MEDIUM finding in research/experiments/design-review-round1.md by editing the affected protocols/specs/index fragments (record a disposition per finding, with justification for any rejection, appended to the review file), rerun research/tools/experiments/build_index.py and ebr validate for changed specs, then commit ALL experiment design artifacts as the pre-registration record via ${CKPT} --subject "research: pre-register Phase C experiment designs" --body "<state that no experiment result for these designs exists at this commit; counts>" --note "<one-line>" -- research/experiments research/tools/experiments. Return a summary with final counts and the commit hash.`, { label: 'revise:design', phase: 'Revise' })

return { designs: designs.map((r, i) => ({ domain: DOMAINS[i].key, n: r && r.experiments.length, ready: r && r.ready, blocked: r && r.blocked, tooling: r && r.needs_tooling, uncovered: r && r.uncovered_decisions })), index: index && String(index).slice(0, 1500), review: review && String(review).slice(0, 2000), revise: revise && String(revise).slice(0, 1500) }
